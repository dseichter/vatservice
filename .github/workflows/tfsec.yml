name: 'tfsec'
on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'

jobs:
  tfsec:
    name: 'tfsec'
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout'
        uses: actions/checkout@v2

      - name: 'tfsec'
        uses: triat/terraform-security-scan@v2.0.2